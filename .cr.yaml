# https://github.com/helm/chart-releaser — no longer used by CI.
# The Helm repo index is now generated by .github/workflows/pages.yml using the
# cr CLI directly and deployed to GitHub Pages via actions/deploy-pages.
# This file is kept for reference only.
pages-branch: gh-pages
